#include "main.h"
/**
 * add - function  adding two integers and returning the sum
 * @a: first  variables to enter
 * @b: second number to enter
 * Return: somme
 */
int add(int a, int b)
{
	int somme = a + b;

return (somme);
}
